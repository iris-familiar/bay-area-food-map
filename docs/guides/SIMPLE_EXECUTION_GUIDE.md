# 简化执行指南 - One Job Per Day

## 🎯 核心概念

**每天只运行一个Job，自动完成所有工作：**

```
⏰ 11:00 AM Daily Master Job
├── 1. 检查博主更新
├── 2. 搜索当天指定地点 (自动轮换)
├── 3. 获取帖子详情
├── 4. 挖掘评论区餐厅
├── 5. Google验证
├── 6. 更新数据库
└── 7. 生成日报
```

## 🚀 快速开始

### 安装自动化 (一次性)
```bash
cd ~/.openclaw/workspace-planner/projects/bay-area-food-map

# 安装简化版cron (每天11点运行)
crontab config/crontab_simple.txt

# 验证安装
crontab -l
```

### 查看状态
```bash
# 今日是否已运行
ls logs/daily_$(date +%Y%m%d).log 2>/dev/null && echo "✅ 今日已完成" || echo "⏳ 等待11:00运行"

# 实时查看日志
tail -f logs/daily_$(date +%Y%m%d).log

# 查看Dashboard
open admin.html
```

### 手动运行 (如果需要)
```bash
# 立即运行今天的任务
bash scripts/daily_master_job.sh

# 或者指定特定地点
bash scripts/end_to_end_batch.sh "Fremont美食"
```

## 📅 自动轮换地点

| 星期 | 自动搜索 | 预期发现 |
|------|---------|---------|
| 周一 | Cupertino美食 | 3-5家 |
| 周二 | Fremont美食 | 3-5家 |
| 周三 | Milpitas美食 | 3-5家 |
| 周四 | Mountain View美食 | 2-4家 |
| 周五 | Palo Alto美食 | 2-4家 |
| 周六 | 南湾美食 | 2-4家 |
| 周日 | 湾区美食 | 2-4家 |

**每周预期**: 15-25家新餐厅

## 📊 监控进度

### Admin Dashboard (admin.html)
- 实时显示当前批次状态
- 新发现的餐厅候选
- 验证进度

### 文件检查
```bash
# 今日数据
cat data/batch*_$(date +%Y%m%d)*_candidates.json

# 今日日志
cat logs/daily_$(date +%Y%m%d).log

# 数据库状态
cat data/current/restaurant_database.json | grep '"id": "r' | wc -l
```

## ✅ 完成标准

每天11:00后检查：
1. ✅ logs/daily_YYYYMMDD.log 存在
2. ✅ raw/batch*_YYYYMMDD_*.json 新增5个文件
3. ✅ 数据库候选餐厅数增加
4. ✅ admin.html 显示最新状态

## 🆘 故障排除

### Job没运行
```bash
# 检查cron是否安装
crontab -l

# 手动运行测试
bash scripts/daily_master_job.sh
```

### Rate limit错误
- 系统自动处理：等待并重试
- 查看日志：tail -f logs/daily_*.log

### 数据没更新
```bash
# 检查Lock文件
ls .daily_job.lock && echo "Job正在运行" || echo "Job未运行"

# 删除lock (如果卡住)
rm .daily_job.lock
```

## 📈 预期进度

| 日期 | 累计餐厅 | 里程碑 |
|------|---------|--------|
| Day 7 (2/22) | ~40家 | Week 1完成 |
| Day 14 (3/1) | ~60家 | Week 2完成 |
| Day 21 (3/8) | ~80家 | Week 3完成 |
| Day 28 (3/15) | 100家 | 🎯 目标达成 |

---

**只需要做一件事：确保每天11:00前电脑在线，让cron自动运行！**
